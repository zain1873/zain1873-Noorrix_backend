[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --no-input && python manage.py seed_faqs && python manage.py ensure_admin && python manage.py shell -c \"from django.contrib.auth import get_user_model; U=get_user_model(); u=U.objects.filter(email='admin@noorrix.com').first(); print('PRE-GUNICORN CHECK - user:', u.email if u else None, '| pw_ok:', u.check_password('Noorrix2024') if u else False, '| db:', __import__('django').db.connections['default'].settings_dict['HOST'])\" && python manage.py collectstatic --no-input && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
